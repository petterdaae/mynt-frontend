import styled from 'styled-components';

const Wrapper = styled.div`
  text-align: center;
`;

function Loading() {
    return (
        <Wrapper>
            Loading
        </Wrapper>
    );
}

export default Loading;

